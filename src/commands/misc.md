# 其它指令

## `/dcs`：Discord消息同步 {#dcs}

```text
/dcs start <guild-id>.<channel-id>
/dcs stop
```

开始将本群的消息与指定 Discord 频道进行双向同步。
`<guild-id>` 和 `<channel-id>` 分别是 Discord 服务器和频道的 ID，
格式为 `123456789012345678.987654321098765432`。

使用此功能需要在 Discord 端安装并配置 Seira 的 Discord Bot（目前暂未公开提供）。
